spring:
  datasource:
    password: postgres
    url: jdbc:postgresql://localhost:5432/servermanager
    username: postgres
  jpa:
    hibernate:
      ddl-auto: create
    properties:
      hibernate:
        format_sql: true
        dialect: org.hibernate.dialect.PostgreSQLDialect
    show_sql: true